defaultNamespace: kubecost
labels:
  apps: kubecost

helm:
  chart: kubecost
  repo: https://kubecost.github.io/kubecost	
  releaseName: kubecost
  version: 3.0.0
dependsOn:
  - selector:
      matchLabels:
        apps: monitoring
