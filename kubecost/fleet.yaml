defaultNamespace: kubecost
labels:
  apps: kubecost
dependsOn:
  - selector:
      matchLabels:
        apps: monitoring
helm:
  chart: kubecost
  repo: https://kubecost.github.io/kubecost	
  releaseName: kubecost
  version: 3.0.0
